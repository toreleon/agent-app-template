node_modules
.next
out
build
dev.db
dev.db-journal
# Isolated Sites mini-app datastore (prisma/sites-data.prisma) + its generated
# client. Second SQLite DB; never committed. See src/lib/sites/data-db.ts.
sites-data.db
sites-data.db-journal
sites-data.db-shm
sites-data.db-wal
/src/generated
# Per-Site uploaded files (src/lib/sites/blob.ts). Runtime data; never committed.
/.sites-blob
.env
.env.local
/uploads
# User uploads are written to public/uploads (see src/lib/storage.ts); keep the
# directory but never commit uploaded files.
/public/uploads/*
!/public/uploads/.gitkeep
# Per-conversation coding-agent sandboxes (src/lib/sandbox/confine.ts). Scratch
# working trees — never commit them.
/.workspaces
# Installed Claude plugins (src/lib/plugins/*). Cloned/copied per user — never
# commit them.
/.plugins
/.playwright-mcp
*.log
.DS_Store
next-env.d.ts
